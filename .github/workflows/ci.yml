name: ci

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install -U pip
          pip install -U ruff pylint mypy pytest httpx fastapi uvicorn
          pip install -U transformers sentence-transformers torch --index-url https://download.pytorch.org/whl/cpu

      - name: Ruff
        run: ruff check .

      - name: Pylint
        run: pylint src

      - name: Mypy
        run: mypy src

      - name: Pytest (unit tests)
        env:
          RUN_MODEL_TESTS: "0"
        run: pytest
